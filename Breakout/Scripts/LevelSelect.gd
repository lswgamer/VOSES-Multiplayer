extends Control


#onready var grid = $Panel/GridContainer

# Called when the node enters the scene tree for the first time.
func _ready():
	#grid.h_separation = int($Panel/GridContainer/LS0/LevelPreviewButton.texture_normal.get_size().x)
	#grid.v_separation = int($Panel/GridContainer/LS0/LevelPreviewButton.texture_normal.get_size().y)
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
