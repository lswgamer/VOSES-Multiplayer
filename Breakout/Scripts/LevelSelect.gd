extends Control


#onready var grid = $Panel/GridContainer

# Called when the node enters the scene tree for the first time.
func _ready():
	#grid.hseparation = int($Panel/GridContainer/LS0/LevelPreviewButton.texture_normal.get_size().x)
	#grid.vseparation = int($Panel/GridContainer/LS0/LevelPreviewButton.texture_normal.get_size().y)
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
